股票预览浏览调整。
1. 数据的呈现不使用表格，而是使用常规股票软件（如同花顺）中的K线图浏览方式。我需要从上往下依次查看，1分钟K（默认2天窗口），日K（默认60天），周K（默认360天）数据。
2. 当光标在对应K线时候，光标呈现十字星，光标在K线上的蜡烛图时，输出当前柱的K线指标，并且通过按下上下键，对光标所在K线进行放大缩小。
3. 默认情况下1分钟K数据每间隔5分钟获取一次，每次获取最近2天(1440分钟)，日K和周K，每天(24小时)获取一次。当点击“更新数据”按钮，立刻获取当前股票的最新数据。
4. 去掉AI分析按钮。